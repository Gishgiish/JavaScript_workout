// Math = built-in object that provides a collection of properties and methods

let x = 3.99;
let y = 2;
let z;
let p;

z = Math.round(x); // rounds the number up or down depending on the value
p = Math.floor(x); // rounds the number down to the whole number
q = Math.ceil(x); // rounds up the number to the nearest whole number

// others include: trunc(truncates to the whole number), pow(calculate the power of a number takes two arguments the number ad value to power up)
//                 sqrt(for square root)